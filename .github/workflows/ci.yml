name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      # Before `npm ci`, because it needs no dependencies and this is the
      # cheapest check in the repo — seconds, and it fails before anything is
      # installed. `.claude/skills/phong-ship-check` maps a changed file to the
      # browser suites it puts at risk, and that map is a hand-written claim
      # about eighteen suites: it drifted four times on its first day, always
      # toward selecting too FEW, which is the direction that lets a targeted
      # run report success while skipping the flow the change actually broke.
      # This regenerates the floor from what the suites really drive and fails
      # when a rule sits below it, so the map cannot rot unnoticed between the
      # moments somebody remembers to check it.
      - name: Suite map still true
        run: npm run lint:suites

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run lint

      # Coverage rather than a bare `npm test`: same suites, plus the floors on
      # the shared pure logic. There is no global threshold — src/components is
      # Playwright's job (see the `e2e` job below), and a global number would
      # either fail on that or be set low enough to measure nothing. The floors
      # and the reasoning live in vite.config.ts.
      - name: Test + coverage floors
        run: npm run test:coverage

      - name: Build
        run: npm run build

  # The browser suites drive a real Chromium against a real server, so they
  # cost minutes where `verify` costs seconds. Kept as its own job: it runs
  # alongside `verify` rather than in front of it, and a red one names the
  # broken flow instead of a broken build.
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Keyed on the lockfile, so a playwright-core bump fetches the matching
      # browser instead of running against a stale one.
      - name: Cache Chromium
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install Chromium
        run: npx playwright-core install --with-deps chromium

      # The suites run against `node dist/server.cjs` — the production entry,
      # not the dev server — so a build is a precondition, not a side errand.
      - name: Build
        run: npm run build

      - name: Browser E2E
        run: npm run test:e2e
