# PORT: HTTP + WebSocket listen port. Render injects this automatically.
PORT=3000

# DATA_DIR: Directory for the JSON game database. Defaults to ./data.
# In production point this at a persistent volume (Render disk: /data),
# otherwise every deploy wipes player profiles, ELO, and match history.
DATA_DIR=./data
